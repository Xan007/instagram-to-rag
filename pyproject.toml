[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "instarag"
version = "0.2.0"
description = "Extract knowledge from Instagram profiles and saved posts into a vector database for RAG."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "apify-client>=3.1.3",
    "faster-whisper>=1.2.1",
    "google-genai>=2.19.0",
    "openai>=3.3.1",
    "pinecone>=9.1.0",
    "python-dotenv>=1.2.3",
    "rank-bm25>=0.2.2",
    "reportlab>=5.0.1",
    "rich>=14.0.0",
    "sqlalchemy>=2.0",
    "typer>=0.27.1",
    "yt-dlp>=2026.8.19",
]

[project.scripts]
instarag = "main:main"
instarag-api = "src.api.main:main"

[dependency-groups]
dev = [
    "pytest>=8.3",
    "alembic>=1.13",
]

[project.optional-dependencies]
api = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "python-multipart>=0.0.9",
    "httpx>=0.27",
]

[tool.setuptools]
py-modules = ["main", "api"]

[tool.setuptools.packages.find]
include = ["src*", "config*", "storage*"]
